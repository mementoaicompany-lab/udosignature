# 검수 기록 — 2026-09-20 전면 개선

## 정적·SEO 검사

`python3 build.py`, `python3 scripts/check.py`, JavaScript 3개 구문 검사 통과. HTML 8개, 검색 대상 6개. 고유 title/description/H1/canonical, JSON-LD, 사이트맵, 이미지 alt·크기, `/udosignature/` 경로의 내부 링크·앵커, guide·404 noindex를 검사했습니다. 전체 공개 파일 약 1.96MB이며 페이지별 필요한 파일만 요청합니다. 실제 인물 차량 사진, Sites 설정·CNAME·Firebase SDK·관리자 코드·쓰기 API가 없습니다.

## 브라우저 검사

320·390·768·1440px × 7페이지 = 28조합에서 가로 넘침 없음, H1 하나, 로드된 이미지 실패 없음. PC와 모바일 첫 화면, 지도 장소 선택, 제휴 쿠폰 펼치기, 배시간 화면을 시각적으로 확인했습니다. 주행 장면의 멈춤·재생은 실제 버튼 클릭 후 animationPlayState가 paused/running으로 바뀌는 것을 확인했습니다. 지도 장소 선택 시 해당 카드만 보이며 제목으로 키보드 초점이 이동합니다. 주 메뉴는 모바일에서 가로 스크롤됩니다.

배시간 새로고침에서 기존 과거 날짜 데이터가 ‘당일 운항 확인 필요’로 표시됨을 확인했습니다. `node scripts/check-ferry.cjs`의 16개 assertion으로 정상·결항·단축·막배 이후·한국시간 날짜 변경·오래된 결과·잘못된 시각을 검사했습니다. 기기 위치 권한은 임의로 허용하지 않았으므로 실제 GPS 좌표 검수는 포함하지 않습니다. 외부 예약 결제, 광고 집행, 실사용자 Core Web Vitals도 이번 검사 범위 밖입니다.

## 기존 사이트 분리

기존 GitHub HTML 기준 SHA-256: `14c430d7706f75541ee2e979a3807c29bd523cf2270713750902c5c9851fd445`. 기존 소스·저장소·배포·관리 화면은 수정하지 않았습니다. 단일 공개 운항 기록만 GET으로 읽습니다. 새 사이트는 기존 데이터·방문 카운터에 쓰지 않고 분석 이벤트도 udosignature 메모리 공간으로 분리합니다.

## 게시 전후

GitHub Pages main/docs 대상으로 이 저장소에서만 빌드합니다. 배포 후 각 URL 직접 접속·새로고침, 공개 자산 HTTP 상태와 빌드 파일 일치, 기존 사이트 HTML 무변경을 별도로 확인합니다. 운영자 입력 항목과 검색·광고 등록은 README 및 SEO.md에 기록합니다.

## 네이버 모바일 SEO 추가 검수

2026-09-20: `/udo/` 추가, 여행·차량 검색 의도와 운영자 확인 요금을 반영했습니다. 8개 페이지 × 320/390/768/1440px = 32조합에서 넘침·로드된 이미지 실패·H1 오류가 없었습니다. 모바일 고정 ‘요금·조건’ 버튼으로 요금표에 이동하는 것을 확인했습니다. 정적 HTML 9개, 로컬 참조 365개, 검색 대상 7개, 내부 링크·앵커·구조화 데이터·HTML 중첩·날짜 검사 통과. 배시간 16개 assertion 유지.

IndexNow는 독립 경로의 공개 키 파일만 사용합니다. 전송 스크립트는 별도로 명시적 실행하며, 배포 전에는 dry run만 수행합니다. 검색어 지도는 29개 기본·조합 검색어이며 검색량·순위를 측정했다고 주장하지 않습니다. 실제 서치어드바이저 등록 화면에서 하위경로의 호스트 단위 등록 제한을 확인했습니다.
